/** 
 * Custom JavaScript for the Ivory Tower theme. * 
 */

jQuery( document ).ready(function( $ ){
	// Target an appropriate wrapper on item(s) for FitVids.js for fluid video embedding.
	$(".entry").fitVids();
});